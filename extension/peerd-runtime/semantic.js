// @ts-check
// Pure semantic-route public surface. Browser/storage authority stays in the
// kernel; the sealed host receives only privacy-projected values.

export { mergeContacts } from './contacts/aggregate.js';
