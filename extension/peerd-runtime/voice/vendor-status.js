// @ts-check
// Lightweight sentinel rewritten by scripts/vendor-moonshine.sh alongside the
// pinned runtime. Capability probes must not import Moonshine just to read it.

export const MOONSHINE_VENDORED = true;
